//SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

/// @notice Interface for pool hooks
interface IHooks {
    // This is a marker interface - hooks interface vary by pool type
    // Each pool type (CL, Bin) has its own specific hooks interface
} 